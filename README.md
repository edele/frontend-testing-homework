# frontend-testing-homework

## Setup

To initialize the react app, type `npm install`. After that, you can run the app with `npm start` command. To perform testing scripts, type the following command `npm test`. 
